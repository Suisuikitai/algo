#include <bits/stdc++.h>
#include <regex>
using namespace std;

int main(int argc, char const *argv[])
{
	int N;
	string Y, M;
	cin >> N >> Y >> M;
	string S;
	regex reg{R"(\d+_.+_)"};
	for (int i = 0; i < N; i++)
	{
		cin >> S;
	}
	return 0;
}
