#include <bits/stdc++.h>
using namespace std;

//図に起こす
vector<int> mergesort(vector<int> A)
{
}

int main(int argc, char const *argv[])
{
	int N;
	cin >> N;
	vector<int> A(N);
	for (int &a : A)
		cin >> a;
	return 0;
}
